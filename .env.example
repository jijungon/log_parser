# docker-compose 환경변수
# 복사 후 사용: cp .env.example .env

# log_parser → test-server push 토큰 (양쪽에서 동일하게 사용됨)
PUSH_OUTBOUND_TOKEN=test-token

# POST /flush 인증 토큰
FLUSH_INBOUND_TOKEN=

# test-server 호스트 노출 포트 (컨테이너 내부는 8080 고정, 호스트 충돌 시 여기서 변경)
RECV_PORT=8080

# 로그 레벨
RUST_LOG=info
