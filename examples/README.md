# examples

실제 에이전트가 생성한 envelope 응답 샘플.

| 파일 | 내용 |
|------|------|
| `envelope-response.00.example` | `log_batch` envelope — push 응답 전체 구조 |
| `normalizer-dedup.00.example` | DedupEvent 정규화·중복 제거 출력 샘플 |
| `stat-report-response.00.example` | `stat_snapshot` envelope — `GET /stat` 응답 |
| `sos-report-response.00.example` | `sos_snapshot` envelope — `POST /trigger-sos` 응답 |

데이터 구조 설명은 루트 `README.md`의 **데이터 구조** 섹션을 참고하세요.
