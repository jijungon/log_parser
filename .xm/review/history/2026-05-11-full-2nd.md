# x-review: full — Block 🚫
- Date: 2026-05-11
- Branch: (no git repo)
- Lenses: security, logic, perf, tests
- Agents: 4
- Findings: 31 (Critical: 0, High: 9, Medium: 14, Low: 7)

---

## Verdict: Block 🚫
0 Critical / 9 High. collect.rs 블로킹 I/O 4건, dedup 경계 버그, 재시도 off-by-one, stat/sos/check_auth 테스트 누락.

이전 리뷰(2026-05-11) Critical 2건 → 0 해소. High 8 → 9 (기존 perf 유지 + 신규 tests 3건).

## Critical (0)

없음 ✅

## High (9)

**[High] coordinator/mod.rs:214** — 재시도 off-by-one: `attempt + 1 >= max_retries`로 max_retries=5 시 4회만 retry (tests)
→ Fix: `attempt >= max_retries as u64`로 변경

**[High] dedup/window.rs:49-50** — push는 `<=` 사용, flush_expired는 `>` 사용 — 경계값 이벤트 오분류 (logic)
→ Fix: `elapsed <= self.window_secs` → `elapsed < self.window_secs`

**[High] inbound/collect.rs:293-326** — /proc 전체 동기 스캔이 async executor thread 점유, spawn_blocking 없음 (perf)
→ Fix: tokio::task::spawn_blocking으로 감싸기

**[High] inbound/collect.rs:403** — 프로세스 루프 내 /proc/{pid}/fd readdir (300+ PID × syscall, async 블로킹) (perf)
→ Fix: /proc/{pid}/status의 FDSize 파싱으로 대체

**[High] inbound/collect.rs:330-336** — /proc/meminfo 동일 요청 내 3회 중복 읽기 (perf)
→ Fix: 최상단 1회 읽어 하위 함수에 파라미터 전달

**[High] inbound/collect.rs:524-544** — 인터페이스 루프 내 /sys/class/net/{iface}/* 동기 읽기 (perf)
→ Fix: spawn_blocking으로 감싸기

**[High] inbound/stat.rs:14** — handle_stat 핸들러 테스트 전무 (tests)
→ Fix: 401/200 케이스 테스트 추가 (flush 패턴 동일 적용)

**[High] inbound/sos.rs:14** — handle_sos 핸들러 테스트 전무 (tests)
→ Fix: 401, bypass, 200 케이스 테스트 추가

**[High] inbound/mod.rs:40** — check_auth 직접 단위 테스트 없음 (3개 endpoint 공용 인증 함수) (tests)
→ Fix: mod.rs에 #[cfg(test)] 블록 추가 (None/prefix 누락/길이 불일치/정상 4케이스)

## Medium (14)

| 파일 | 내용 | 렌즈 |
|------|------|------|
| inbound/flush.rs:39 | !st.flush_token.is_empty() && bypass — 빈 문자열 토큰 시 인증 우회 | security |
| inbound/collect.rs:726 | /etc/hosts 내용 그대로 envelope 포함 → 내부 네트워크 토폴로지 노출 | security |
| pipeline/vector_spawn.rs:30 | cfg.vector_bin 검증 없이 Command::new() 전달 | security |
| transport/spool.rs:36 | prev + json_len u64 overflow 가능 (challenged ↓ from High) | logic |
| dedup/window.rs:126 | ts_last: Utc::now() — 이벤트 타임스탬프 대신 wall clock 사용 | logic |
| inbound/flush.rs:32 | retry_after_secs() 0 반환 가능 (클라이언트 즉시 재시도 유발) | logic |
| pipeline/vector_spawn.rs:63 | crash 카운트 push 후 즉시 체크 → max_restarts-1번만 실제 허용 | logic |
| normalize/tokens.rs:58 | 7개 regex 순차 + 매 pass into_owned() 할당 | perf |
| coordinator/mod.rs:96 | format! heap 할당 매 이벤트 fingerprint 계산 | perf |
| dedup/window.rs:86 | flush_expired 50K LRU 전체 순회 (5초마다) | perf |
| inbound/collect.rs:976 | SOS 로그 파일 처음부터 전체 읽기 (수십MB, blocking) | perf |
| inbound/flush.rs:32 | retry_after_secs 단언이 > 0만 확인 | tests |
| inbound/collect.rs:169 | is_partition() heuristic 테스트 없음 | tests |
| normalize/tokens.rs | strip_syslog_prefix 테스트 없음 | tests |

## Low (7)

| 파일 | 내용 | 렌즈 |
|------|------|------|
| inbound/collect.rs:976 | 로그 파일 open 시 O_NOFOLLOW 미사용 | security |
| transport/http.rs:19 | 아웃바운드 토큰 비어있어도 warn-only | security |
| coordinator/mod.rs:67 | seq 시작값 1 하드코딩 (seq_state_path 미로드) | logic |
| transport/http.rs:38 | retry마다 JSON 직렬화 + gzip 재실행 | perf |
| inbound/collect.rs:340 | /proc/stat 중복 읽기 | perf |
| transport/spool.rs | pending() 정렬 순서 및 load() corrupt 케이스 미테스트 | tests |
| platform/cgroup.rs | self_attach/attach_pid/verify 테스트 없음 | tests |

## Summary

| Lens | Total | Critical | High | Medium | Low |
|------|-------|----------|------|--------|-----|
| security | 5 | 0 | 0 | 3 | 2 |
| logic | 7 | 0 | 2 | 4 | 1 |
| perf | 10 | 0 | 4 | 4 | 2 |
| tests | 9 | 0 | 3 | 3 | 2 |
| **Total** | **31** | **0** | **9** | **14** | **7** |
